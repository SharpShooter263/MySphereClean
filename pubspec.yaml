name: mysphereclean
description: MySphere profil yönetim uygulaması.

publish_to: "none" # Uygulamayı pub.dev'e yüklemeyeceğiz

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # 🔥 Firebase paketleri
  firebase_core: ^3.10.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4

  # 🌐 Link açma
  url_launcher: ^6.3.0

  # 📷 QR kod üretme
  qr_flutter: ^4.1.0

  # 📤 Paylaşım (link/QR paylaşma için)
  share_plus: ^10.0.0

  # 💾 Küçük ayarları (dark mode vb.) cihazda saklamak için
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
